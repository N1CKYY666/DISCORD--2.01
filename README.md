# DISCORD--2.01
Basic discord bot for practicing and incorporate AI.

python version: 3.11 (USE THIS ONE)

Yoy may need these dependencies. They'll ensure the bot is working well:
pip install discord.py
pip install numpy
pip install keras
pip install requests
pip install tensorflow==2.12.0 (USE THIS VERSION!)
pip install Pillow

